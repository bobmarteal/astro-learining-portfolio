---
import { socialData } from "@/data/siteData";
import { Icon } from "astro-icon/components";
// console.log(socialData);
---
<footer>
    <div>
    <p> <span id="copyright">{new Date().getFullYear() -1 }</span> Katie Haus. All rights reserved. I’m a skilled web developer passionate about creating innovative and user-friendly websites.</p>

    <ul>
        {
            socialData.map(
            (i) =>
            (
                <li><a href={i.href} aria-label={i.title}><Icon name={i.icon} size={24}></Icon></a></li>
            )
            ) 
        }
    </ul>
    </div>
</footer>
<script>
     const copyrightSpan = document.querySelector("#copyright") as HTMLSpanElement;
     copyrightSpan.textContent = new Date().getFullYear().toString();
</script>